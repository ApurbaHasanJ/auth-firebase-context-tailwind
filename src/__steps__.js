/**
 * Context API
 * 1. create context (export)
 * 2. create provider
 * 3. set context value
 * 4. to access the value: useContext hook
 */

/**
 * Share context
 * 1. main.jsx set AuthProvider
 * 2. inside provider access children props nd then use 
 */

/**
 * Integrate
 * 1. breakdown the create user function
 * 2. auth provider will have auth and password for register
 */